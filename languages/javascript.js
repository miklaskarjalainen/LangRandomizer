const JavaScript = {
    name: "JavaScript",
    style: "javascript",
    example: `function sum(a, b) {
    return a + b;
}

const A = 8;
const B = 7;
console.log(\`\${A} + \${B} = \${sum(A, B)}\`);`
};
add_langugage(JavaScript);
