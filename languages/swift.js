const Swift = {
    name: "Swift",
    style: "swift",
    example: `func sum(_ a: Int, _ b: Int) -> Int {
    return a + b
}

let A = 8
let B = 7
print("\\(A) + \\(B) = \\(sum(A, B))")`
};
add_langugage(Swift);
