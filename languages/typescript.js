const TypeScript = {
    name: "TypeScript",
    style: "typescript",
    example: `function sum(a: number, b: number): number {
    return a + b;
}

const A: number = 8;
const B: number = 7;
console.log(\`\${A} + \${B} = \${sum(A, B)}\`);
`
};
add_langugage(TypeScript);
