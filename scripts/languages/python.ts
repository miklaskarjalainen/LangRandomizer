const Python: ProgrammingLanguage = {
  name: "Python",
  style: "python",
  example:
`def sum(a, b):
    return a + b

if __name__ == "__main__":
    A = 8
    B = 7
    print(f"{A} + {B} = {sum(A, B)}")`
};

add_langugage(Python);
